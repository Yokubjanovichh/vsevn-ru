# FE KONTEKST — vsevn.ru «Событие 2» (frontend ishchi-konteksti)

> Bu fayl yangi sessiya ishni xatosiz davom ettirishi uchun. Loyiha: kwork,
> «Отправленные резюме» kabineti sahifasi. Menejer TЗ beradi → bitta qadam
> bajariladi → DOKLAD (kodsiz) → TO'XTASH. Brauzerда tekshirilmaydi (vizualni
> foydalanuvchi tekshiradi); o'z-nazorat — PIL/fontTools bilan etalon o'lchash
> va dasturiy testlar. Til: o'zbekcha doklad, ruscha kod-kommentlar.

## 0. Qat'iy qoidalar
- Faqat toza HTML/CSS/JS. jQuery/framework/build TAQIQ. Rastr TAQIQ — hammasi inline-SVG.
- Barcha UI matn statik-dizayn dvigateli orqali (SVG `<text>`); istisno — qidiruv `<input>` (placeholder native).
- Pixel-perfect: o'lchamlar `calc(N * var(--dpx, 0.0520833vw))` (dizayn-px @1920).
- Data-driven: jadval/popап/chartlar JS massivlardan.
- Har o'zgarishda `node --check js/script.js`; versiya bump (`?v=` ikkalasi index.html'da).
- DOKLADGA KOD TASHLANMAYDI. Har adaptiv-dokладда desktop regress-suite (5-bo'lim).

## 1. Tuzilma va kalibrlar
```
index.html            — butun markup (sidebar, hero, mid-row, jadval, popап, tab-view)
css/style.css         — v=483 (keyingi bump: 484); js v=372 (keyingi: 373). **TЗ-28** (scroll-top «вверх» Figma 1:1 + HTML-migratsiya): (1) HTML: ikona yangi strelka path (`M9.09326 0...`) 19×12 viewBox, inline o'lcham 21×15dpx, fill #F8EDD0. (2) JS `initScrollTop` matn SVG → HTML-text (Onest 400/21/lh1, rang CSS dan). measureTextWidth/renderElementText olib tashlandi. (3) CSS `.scroll-top`: w 81→**80**, h 52.6→**53**, border-radius 18→**12.2**dpx, fon `rgba(161,155,140,.92)`→**#7C7971**, `.is-visible opacity 1→**0.8**`. Hover: fon `rgba(140,135,122,.95)`→**#2F3027**, `svg path fill #FFFBEF`, `.scroll-top-text color #FFFBEF` (default #F8EDD0 CSS). TЗ-19/19-fix-1 (opacity/PIL fon) bekor. (4) Mobil (<800px): dead `.scroll-top-text .svg-label` → `.scroll-top-text { font-size:15px }` (real-px, HTML-text vw-calc kichrayishidan himoya). css v=565→**566**, js v=464→**465**. `node --check` PASS. **TЗ-27** (mijoz): `below-right` ikkala tooltip (Очистить поле + Деактивировать) ishlatardi — faqat resume-filter-x ni ko'tarish uchun yangi `data-tip-pos="below-right-high"` (gap 21.8→**16.8**dpx, 5px yuqoriga) `initUiTipHover` ga qo'shildi; 2 ta «Деактивировать» tooltip (resume+pub) shu pos'ga o'tkazildi. «Очистить поле» below-right (21.8) tegmadi. js v=463→**464**. **TЗ-26** (mijoz 3 pravka): (1) tooltip Figma 1:1 — `UI_TIP_INLINE_STYLE` (js) + `.ui-tip` (css): `border-radius 4dpx→0` (to'g'ri burchak), `padding 8.1/5.9 → 10/12` (balandroq + matn ~1dpx yuqoriga), `display:flex + align-items/justify-content center` (vertikal markazlash). Fon #BDBBB7 / Onest Light 21.6 / qora (TЗ-10-fix6) saqlandi. Barcha `.ui-tip` (Очистить поле/Деактивировать.../Выйти) — bir xil; cowboy/snav-bell native-title tegmadi. (2) Выйти hover `#DD5326→#FF3F01`: `.snav-exit:hover .snav-exit-icon svg path { fill:#ff3f01 }` (ikona kontur, rect transparent saqlandi) + yangi `.snav-exit:hover .snav-exit-text { color:#ff3f01 }` (so'z — TЗ-17 HTML-text color CSS dan). css v=564→**565**, js v=462→**463**. **TЗ-25** (mijoz pravkasi): `.resume-clear-btn` (desktop resume+pub qidiruv ✕) `top: 9.9→8.9dpx` (1px yuqoriga, vertikal markazlash). left/boshqa tegmadi. css v=563→**564**. **TЗ-24** (popап «...» hisob-filtri SVG-text → HTML text — OXIRGI SVG-blok): `renderAccPopupTexts` HTML'ga (yangi `_renderAccTextEl` helper). Sarlavha (`.acc-popup-title` 22/300) + 20 hisob-qatori (`.acc-num`/`.acc-date` 20/300) — textContent + inline font/size/weight/lh-calc (DASH_NAV_TEXT_FAMILY Sora→Onest stack). **RANG inline'da YOZILMAYDI** — CSS boshqaradi (POP5/POP6): bazaviy `.acc-popup-title { color:#584E4E }`, `.acc-num/.acc-date { color:#86868A }`; holat eski `.svg-label text { fill }` → `.acc-row:hover .acc-num/.acc-date { color:#4D4343 }` + `.acc-row.is-checked ... { color:#0087FC }`. GEOMETRIYA (`layoutAccPopup` --acc-w/--acc-date-x measureTextWidth, top, SEL1, scrollbar, checkbox SVG) TEGMADI. Filtr/accountFilterSet tegmadi. Render-key `data-accHtmlKey`. **BUTUN SAHIFA endi HTML-text — SVG-text engine matn uchun ishlatilmaydi, resize-bug butunlay yopildi.** css v=562→**563**, js v=461→**462**. `node --check` PASS. **TЗ-23** (tab-view mobile/planshet SVG-text → HTML text): 3 ta render-funksiya BIR XIL signatura bilan HTML'ga qayta yozildi (barcha call-site'lar — initTabView/initTabPub/applyResumeSearch/applyPubSearch — o'zgarmadi): (1) `renderTabText` — pipe `|` → `\n` + pre-line (multiline) yoki nowrap; size/lh = makSize/makLh × koef (dizayn-px@1920) → `font-size: calc(size * --dpx)` viewport-mustaqil; (2) `renderTabHighlightedText` — `.cell-hl` HTML span (TЗ-21 naqshi, oranj #FD6429/oq); (3) `renderTabDigits` — Arial-stack (TAB_DIGIT_FAMILY, 2.4-fix saboq) HTML, nowrap. Yangi `_tabHtmlStyle` helper + render-key `data-tabHtmlKey`. `createSvgText`/`renderElementText`/`measureTextWidth` tab uchun olib tashlandi. wrapTwoLines (defis-kesim TЗ-11) o'zgarmadi — natija `|`-joined → pre-line 2 qator. CSS (grid/flex/baseline-tekislik/align-self/column-gap) tegmadi; flex/grid bola'lar blockify bo'ladi. Filtr/highlight mantiqi (applyResume/PubSearch) tegmadi. RESIZE: --dpx yangilanib avtomatik masshtab — mobile↔planshet↔desktop reload'siz. js v=460→**461**. `node --check` PASS. **TЗ-22** (desktop pub jadval SVG-text → HTML text, TЗ-21 naqshi): (1) `initPubTable` pub kataklar (`#pubRows .pcell-*`) `renderResumeCellHtml` (TЗ-21 helper), CSS-position (PUB_COLS.x/base/maxW) saqlandi, matn HTML + ellipsis. (2) Pub title (`.resume-title.pub-title`) + shapka (`.pub-panel .resume-col`) HTML — `.pub-panel` scope (TЗ-21 da resume scope'dan ajratib qoldirilgan edi). (3) HIGHLIGHT (8-X): `applyPubSearch` desktop `.pcell-vac`/`.pcell-url` → `renderResumeCellHtml(...ranges)` (`.cell-hl` span). `findAllRanges` saqlandi. SVG `renderResumeCellText`/`renderHighlightedCellText` endi hech kim ishlatmaydi (resume TЗ-21, pub TЗ-22 ikkalasi HTML) — dead-code, lekin tab-pub mobile (TЗ-24'gача) ulardan foydalanmaydi. Filtr/pubRowMatches tegmadi. js v=459→**460**. `node --check` PASS. **TЗ-21 4-PILOT** (desktop resume jadval SVG-text → HTML text): yangi `renderResumeCellHtml(el, text, size, ranges)` + `_applyResumeCellStyle(el, size, weight)`. (1) Kataklar: `#resumeRows` scope (cell.style.left/top/maxWidth CSS-position SAQLANDI, faqat matn HTML). Font Onest/size/300/#2E2F27/lh1 inline-calc. maxW himoya: `overflow:hidden + text-overflow:ellipsis` (eski clip-path SVG o'rniga `...`). (2) Shapka+title: `.resume-panel:not(.pub-panel)` scope (pub `.resume-col` SVG'da qoladi — TЗ-22) — title 24.92/400, col 22/300. (3) HIGHLIGHT HTML-span: `<span class="cell-hl">` (CSS `.cell-hl { background:#FD6429; color:#FFF; border-radius 4dpx; padding 0/3dpx; margin 0/-3dpx }` — plate ±3dpx, matn-poz saqlanadi). `wordPrefixRanges`/`esc()` saqlandi. (4) `applyResumeSearch` desktop nameCell/vacCell → `renderResumeCellHtml(...ranges)`. `renderResumeCellText`/`renderHighlightedCellText` (SVG) PUB uchun saqlandi. Filtr/accountFilterSet tegmadi. js v=458→**459**, css v=561→**562**. `node --check` PASS. **TЗ-20** (legacy init null-guard, console toza): audit — 12 ta DOMContentLoaded legacy init-funksiyasidan **3 tasi** null-element'ga to'g'ridan-to'g'ri tegib console-xato berardi (`initFilePath:9252`, `initImportButton`, `attachLogAreaHandlers`). Qolganlari allaqachon guard'li (`initCustomSelect`, `initLogPreview`, `updateImportButtonText`, `initImportCheckboxes`, `initDateFilterDefaults`, `applyInitialControls`, `syncSelectedTypeControls`, `renderLogTables`) yoki forEach/window-listener (`initNavTabs`, `initControlTooltips`, `initGlobalTooltipDismiss`) — xavfsiz. **FIX**: 3 funksiya boshiga sodda `if (!el) return` (initFilePath → filepathInput, initImportButton → importBtn, attachLogAreaHandlers → logArea, bound-flag element tasdiqlangach). `_safeInit`/try-catch wrapper QO'SHILMADI (TЗ-13 saboq). VIZUAL/render funksiyalarga tegmadi. js v=457→**458**. `node --check` PASS. **TЗ-19-fix-1** (PIL-o'lchangan aniq ranglar, opacity bekor): TЗ-19 `opacity:0.65` olib tashlandi. FON `rgba(47,48,40,0.5)` → **`rgba(161,155,140,0.92)`** (#A19B8C deyarli solid iliq kulrang), hover `rgba(140,135,122,0.95)`. MATN (JS color) `#F5F2EA` → **`#F8EDD0`** (solid krem). STRELKA SVG path fill `#F5F2EA` → **`#F8EDD0`**. css v=559→**560**, js v=455→**456**. **TЗ-19** (foydalanuvchi: scroll-top strelka/matn yarim-shaffof): `.scroll-top svg` va `.scroll-top-text` ga `opacity: 0.65` qo'shildi (krem #F5F2EA endi fon `rgba(47,48,40,0.5)` ustiga singib oqish ko'rinadi). Fon tegmadi. SVG-engine render saqlandi (HTML refaktor keyinroq). css v=558→**559**. **TЗ-18-fix-4** (foydalanuvchi: «Посмотреть» punktir to'liq so'zni qoplamaydi): TЗ-18 da 6-fix-19 `--ink-w` olib tashlangach `.vcard-view::after { width: calc(var(--ink-w) * --dpx) }` noaniq edi; TЗ-18-fix-2 da `.vcard-view` `display: block` butun foot enini egallab punktir kengroq chiqardi. **FIX**: (1) JS `.vcard-view` istisno — `display: inline-block` (matn-eniga teng, strelka bilan yonma-yon juft-hover); (2) CSS `::after` `width: calc(--ink-w...)` → `left:0; right:0` (100%, inline-block element matn-eni → to'liq so'z). Punktir uslubi (1.48/3.0 repeating-gradient #161715) saqlandi. css v=560→**561**, js v=456→**457**. `node --check` PASS. **TЗ-18-fix-3** (foydalanuvchi: «Май» pill balandroq/markazda emas): diagnoz — «Май» elementi **ikkala** klassga ega (`month-label month-pill`) → `.month-label:not(.promo-month)` spec ham, `.month-pill` spec ham `_renderMidHtmlEl` ishlatadi. TЗ-18-fix-2 da `isPill = spec.selector.indexOf("month-pill")` faqat 2-spec'da true → 1-spec (label) `display: block` qo'yib qolardi → CSS `display: flex` (markazlash) buzilardi. **FIX**: `isPill = el.classList.contains("month-pill")` (element-klass bo'yicha, spec-selektor emas) + pill bo'lsa `removeProperty("display")` (prior inline block tozalanadi) → CSS flex (align/justify center, min-height 33.17dpx) hukmron. js v=454→**455**. `node --check` PASS. **TЗ-18-fix-2** (foydalanuvchi: TЗ-18-fix-1 dan keyin joylashuv buzildi — rp-num/label yonma-yon, vcard raqam title yonida): diagnoz — `display: inline-block !important` BARCHA elementlarga majburlanib CSS-dagi maxsus display (`.month-pill { flex }`) va blok-oqimni buzdi. SVG-engine'da elementlar `.svg-label { display: block }` edi → margin-top/height bilan vertikal tizilardi; span default `inline` → yonma-yon. **FIX**: `display: inline-block` → **`display: block`** (blok-oqim, vertikal margin-top tiklanadi: rp-num→rp-label, vcard-title→num→view alohida qatorlarda). `.month-pill` ISTISNO (`spec.selector.indexOf("month-pill")` → display tegmaydi, CSS flex saqlanadi — pill-fon matn-eniga moslashadi). nowrap/pre-line mantiqi saqlandi. js v=453→**454**. `node --check` PASS. **TЗ-18-fix-1** (foydalanuvchi: mid-row sarlavhalar 2-qatorga sig'maydi): SVG-engine'da har element width = matn-eni (intrinsic) — HTML'da yo'qoldi → matn parent-eniga to'liq cho'zilib `\n`siz wrap qilardi. **FIX**: `display: inline-block !important` + `white-space: nowrap !important` (pipe yo'q hollarda — 1 qator + intrinsic eni). Pipe bor hollarda (ataylab ko'p qator Figma'da — masalan `.vcard-title`, `.snav-bell-text` 3 qator) `white-space: pre-line` saqlandi (`\n` qator-yangilash). Stage 11 SVG vizual ko'rinishi bilan teng. js v=452→**453**. `node --check` PASS. **TЗ-18 3-PILOT** (mid-row paneller A/B/C + 5 vcard SVG-text → HTML text): yangi `_renderMidHtmlEl(el, spec)` — hero/sidebar PILOT naqshi. Family stack: `spec.family==="figtree"` → `'Figtree', 'Onest', sans-serif`; default → `'Sora', 'Onest', sans-serif` (kirill Onest fallback, TЗ-6 mos). Color inline-style (mid-row matn statik). Pipe `|` → `\n` + `white-space: pre-line` (vcard-title 2-qatorli, .reach-bar-label, ko'p qatorli matnlar saqlanadi). `text-align: center` `anchor: "middle"` hollarda. 19 ta spec barchasi (rp-title/num/label, reach-title/badge/bar/more-word/num/label, promo-num/sub/month, month-label/pill, vcard-title/num/view). renderMidStaticText soddalashtirildi (5 qator: forEach + helper). **6-fix-19 `--ink-w` CSS-var olib tashlandi** — `.vcard-view::after` punktir uchun edi; HTML inline-block matn-eni o'zi bilan boshqaradi. Agar punktir vizual buzilsa — alohida TЗ. measureTextWidth/SVG-engine olib tashlandi mid-row uchun. js v=451→**452**. `node --check` PASS. **TЗ-17 2-PILOT** (sidebar SVG-text → HTML text): sidebar 6 ta element refaktor — `.snav-text` ×4 (20/400/lh26/center), `.snav-bell-text` ×1 (24/600/lh24/center, 3 qator — pipe `\n` + `white-space: pre-line`), `.snav-exit-text` ×1 (24/600/lh28). Yangi `_renderSidebarHtmlEl(el, size, weight, lh, anchor)` helper — TЗ-16 PILOT naqshi. **Color CSS dan keladi** (inline-style'da yozilmadi — `.snav-btn.active` matn-rangini override qiladi, `.snav-btn:hover` ham). pipe `|` ko'p qator hollarda saqlandi (bell uchun majburiy). measureTextWidth/SVG-engine olib tashlandi sidebar uchun. Render-key `data-sidebar-html-key`. js v=450→**451**. `node --check` PASS. **TЗ-16-PILOT-fix-2** (foydalanuvchi: hero-para va hero-stats yopib qolgan): `.hero-stats { margin-top: 26→**38dpx** }`. css v=557→**558**. **TЗ-16-PILOT-fix-1** (foydalanuvchi: hero-para «Наша\nкомпания» manual-break): pipe-split + `\n` mantiqi olib tashlandi (eski SVG-engine qoldig'i). Endi `text = raw.replace(/\s*\|\s*/g, " ").trim()` — pipe bo'sh joyga aylanadi, brauzer auto-wrap. `white-space: pre-line → normal`. js v=449→**450**. `node --check` PASS. **TЗ-16 PILOT** (HERO matn SVG-text → HTML text, viewport-mustaqil refaktor): 5 ta hero element (`.hero-title/subtitle/para/stat-num/stat-label`) `renderElementText` SVG-dvigatelidan voz kechib **native HTML text** ga ko'chirildi. Yangi `_renderHeroHtmlEl(el, spec, raw)`: `textContent = lines.join("\n")` + `white-space: pre-line` (pipe `|` → ko'p qator); inline-style `!important` bilan `font-family: 'Onest', sans-serif`, `font-size/line-height/letter-spacing: calc(N * var(--dpx, 0.0520833vw))` — viewport-bog'liq, resize'da `--dpx` yangilanib avtomatik masshtablanadi. Render-key `data-hero-html-key` (text + spec hash) — takror render bloki. heroTextSpecs spec saqlandi (size/weight/color/lineHeight/ls qiymatlari). canvas measureTextWidth chaqirig'i olib tashlandi — brauzer matn-eni o'zi hisoblaydi. **ONGLI OG'ISH**: docx G7 Firefox «только текст» talabidan ongli og'ish (HTML text — Firefox'da ham to'g'ri render, lekin SVG-dvigatel'ning «brauzer'da bir xil masshtab» kafolati yo'qoladi → refaktor reyestrga yoziladi). PILOT-bosqich: keyingi guruhlar (sidebar/mid-row/jadval/popап) muvaffaq tekshiruvdan keyin. js v=448→**449**. `node --check` PASS. **TЗ-15** (Qadam A — SVG-text viewport-mustaqil): (1) `_tz14BindResize` (`location.reload`) → `_tz15BindResize` (`updateZoomAwareLines()`) — width-resize'da `--dpx` yangilanadi (`pageScale = innerWidth/1920`), debounce 100ms, height-only ignore. Reload yo'q, init-funksiyalar tegmagan (TЗ-13 saboq). (2) `createSvgText` SVG-attribute `width`/`height` `(X/19.2)+"vw"` → **design-px raqam** (`String(width)`). ViewBox `0 0 width height` o'sha-o'sha (design-px). Tashqi o'lcham CSS-style `calc(var(--dpx) * X)` viewport-bog'liq (saqlandi) → resize'da `--dpx` yangilanib SVG style.width avtomatik o'zgaradi → brauzer viewBox koordinatlarini tashqi o'lcham bilan masshtablaydi. Vizual matn proportional saqlanadi. js v=447→**448**. `node --check` PASS. **TЗ-14-fix-1** (avtomatik reload width-resize'da, eski TЗ-14 yetmadi): `_tz14BindResize` `updateZoomAwareLines()` → **`location.reload()`** ga o'zgartirildi. Width-only guard (`window.innerWidth === lastW → return`) — height-only resize (mobil klaviatura/scroll-bar) e'tiborga olinmaydi. Debounce **500ms** (ko'p hodisa → 1 reload). Pixel-perfect saytda init-funksiyalar idempotent emas (TЗ-13 saboq) → reload eng aniq UX yechim. Real foydalanuvchi DevTools rejim almashtirmaydi, faqat dev/mijoz sezadi. js v=446→**447**. `node --check` PASS. **TЗ-14** (ehtiyotkor minimal resize-fix, eski TЗ-13 iteratsiyalari reset qilingach): DOMContentLoaded ichida yangi IIFE `_tz14BindResize` — `window` resize listener (debounce 100ms) FAQAT `updateZoomAwareLines()` chaqiradi (boshqa init-funksiyalarga TEGMAYDI — TЗ-13-fix-1 saboqi: init-lar idempotent emas, qayta chaqirilsa to'liq buziladi). Bu — `--dpx` viewport-bog'liq qiymatini qayta hisoblaydi → CSS `calc(N * var(--dpx))` va SVG-text `style.width = calc(var(--dpx) * X)` avtomatik yangilanadi. Breakpoint-change uchun mavjud matchMedia listener (`initTabView` ichida `tabViewMediaBound` 5468-qator) saqlandi. Eski `window.addEventListener("resize", ...)` (datepicker 2889, dropdown 9125) tegmagan. js v=445→**446**. `node --check` PASS. **TЗ-10-fix8** (foydalanuvchi: native tooltip cowboy/snav-bell'da chiqmaydi): diagnoz — `<a class="snav-logo">` ichida sof SVG (matn yo'q), `aria-label` mavjudligi Chrome'da `title` native tooltip'ni bostirardi (ba'zi versiyalarda) + SVG bola'lar mouse'ni qabul qiladi, hover-state `<a>` aktivlashadi lekin tooltip element-specific (mouse `<svg>` ustida, `<a>` ko'rinmas). **FIX (kombinatsiya, eng ishonchli)**: (1) `<a class="snav-logo">` dan `aria-label` OLIB TASHLANDI — `title` Chrome'da to'liq ustun. (2) Cowboy default va hover SVG ichiga **`<title>Газета «Все Вакансии Нижнего»</title>`** birinchi bola sifatida qo'shildi — SVG-native title spec, brauzer hover'da har doim ko'rsatadi. (3) Snav-bell SVG (`viewBox=23×28`) ichiga ham `<title>` qo'shildi. `<a>` `title` atributi ham saqlandi backup sifatida. SVG-native title a11y ham ta'minlaydi (screen reader o'qiydi). CSS/JS tegmadi. **TЗ-10-fix7** (mijoz: cowboy va Газета karta uchun brauzer NATIVE tooltip): faqat HTML o'zgardi (CSS/JS tegmadi, versiya bump kerakmas — `?v=` parametri faqat CSS/JS uchun). (1) Cowboy `<a class="snav-logo has-ui-tip" aria-label="...">` → `<a class="snav-logo" title="Газета «Все Вакансии Нижнего»" aria-label="...">` — `has-ui-tip` klassi olib tashlandi, `title` qo'shildi. (2) Bola `<span class="ui-tip" data-tip-pos="above-center" data-text="...">` butunlay o'chirildi. (3) Snav-bell `<a class="snav-bell" href="...">` → `+ title="Газета «Все Вакансии Нижнего»"` (avval has-ui-tip ham, .ui-tip ham yo'q edi — faqat title qo'shildi). Boshqa 4 `.has-ui-tip` joyi (snav-exit-icon, 2× resume-clear-btn, 2× resume-filter-x) **SAQLANDI** — ularda TЗ-10-fix6 Figma 1:1 custom plashka mexanizmi davom etadi. `initUiTipHover` JS tegmadi (boshqa tooltiplar uchun ishlatiladi). **TЗ-10-fix6** (mijoz Figma screenshot 1:1, yakuniy spek): Figma'dan o'lchov: yumaloq burchak ~**4dpx**, qora ramka **YO'Q**, fon **#BDBBB7**. fix5 da `background` ishlatilgan edi (shorthand) — mijoz screenshot'da boshqa CSS qoidasi `background-image` orqali override qilgan bo'lishi mumkin → `background-color: #BDBBB7 + background-image: none` ikkalasi alohida. Border-tarkibi qattiqlashtirildi: `border: none + outline: none + box-shadow: none + text-decoration: none` (parent `<a>` `text-decoration` meros qilmaslik uchun). `border-radius: calc(4 * --dpx)` ikkala CSS va JS-inline'da. JS `UI_TIP_INLINE_STYLE` kengaytirildi: barcha critical xususiyat `!important` bilan inline-cssText (CSS yuklanmagan holatda ham ishlaydi). CSS `.ui-tip` default qoidasi ham yangilandi (background-color/border/outline/border-radius/box-shadow + text-decoration explicit). css v=553→**554**, js v=439→**440**. `node --check` PASS. **TЗ-10-fix5** (foydalanuvchi screenshot diagnoz): screenshot matni «Газета "Все Вакансии Нижнего"» = bizning `data-text` qiymati → ko'rinayotgan element BIZNING `.ui-tip` span. LEKIN stili oq fon + qora ramka + element tepasida — bu brauzer **default `<span>` styling**, ya'ni bizning CSS qoidalari (background:#BDBBB7, padding, font-Onest) **qo'llanmagan**. SABAB: brauzer cache eski CSS yuklab turibdi (?v<553 yuklangan). **FIX**: JS-da BARCHA critical vizual xususiyatlarni `cssText` orqali inline-style + `!important` bilan qo'yamiz (CSS-ga umuman ishonmaymiz). `UI_TIP_INLINE_STYLE` konst — background/padding/font-family/font-weight/font-size/color/text-align/white-space/pointer-events/z-index/border/border-radius/box-shadow. Pozitsiya (`top`/`bottom`/`left`/`transform`) `data-tip-pos` (above-center/below-right/default) bo'yicha alohida hisoblanadi. mouseout'da `cssText = ""` bo'shatadi (default `.ui-tip` qoidasi qaytadi). Endi tooltip vizual ko'rinishi CSS-cache holatiga bog'liq emas. js v=438→**439**. `node --check` PASS. **TЗ-10-fix4** (mijoz xali ham «chiqmadi» — fix3 dan keyin): JS hover + `.is-shown` klass + CSS `!important` ham yetmadi. Eng kuchli yondashish — **INLINE STYLE** (specificity 1000, har qanday CSS override qila olmaydi): `initUiTipHover` mouseover'da `tip.style.display = "block"` + `opacity = "1"` + `visibility = "visible"` qo'shadi; mouseout'da bo'shatadi. `.is-shown` klass ham qoldi (debug uchun). Agar tooltip hali ham ko'rinmasa, sabab brauzer cache (yangi `?v=438` yuklanmayapti) — hard reload (Ctrl+Shift+R) kerak. js v=437→**438**. `node --check` PASS. **TЗ-10-fix3** (mijoz: tooltip xali ham chiqmayapti, TЗ-10-fix2 dan keyin ham): pure-CSS `.has-ui-tip:hover > .ui-tip { display:block }` noaniq sabab bilan ishlamadi — specificity konflikti, bola-tartibi yoki brauzer-specific xato bo'lishi mumkin. **YANGI YONDASHUV (JS-bilan-boshqarish)**: `initUiTipHover` qo'shildi — document-level `mouseover`/`mouseout` event delegation orqali `.has-ui-tip` ostida `.ui-tip` ga `.is-shown` klass qo'shadi/olib tashlaydi. CSS: yangi qoida `.has-ui-tip:hover > .ui-tip, .ui-tip.is-shown { display: block !important }` (eski qoida ham backup sifatida saqlandi). `!important` boshqa qoida tomonidan override qilinishidan himoya. mouseout-da `e.relatedTarget` owner ichida bo'lsa saqlanadi (bola elementlar ustida sirpanganda tooltip yashirinmaydi). DOMContentLoaded'da `renderUiTips` + `initUiTipHover` + `initUiTipClamp` ketma-ketlik. Eski `fonts.ready` qayta-chaqiruv olib tashlandi (TЗ-10-fix2 da native text → font yuklash kerakmas). css v=552→**553**, js v=436→**437**. `node --check` PASS. **TЗ-10-fix2** (mijoz: tooltip xali ham hech qaerda chiqmayapti — fonts.ready fix yetmadi): chuqurroq diagnoz — SVG-text engine (`createSvgText`) tooltip uchun zaif bo'g'in: (1) `textRenderMode = "bitmap"` default rejimi, `useBitmapText: false` bilan svg-branch'ga o'tadi, lekin render-key `getTextRenderEngineKey` bitmap-spec'da → mosishmaslik; (2) `measureTextWidth` Onest yuklash holatiga juda sezgir; (3) plashka eni SVG width'ga bog'liq — width=0 yoki kichik bo'lsa plashka padding ichida bo'sh. **YECHIM (radikal)**: SVG-text engine OLIB TASHLANDI tooltip uchun, **native text-rendering** o'rnatildi: CSS `.ui-tip` ga `font-family/size/weight/color/text-align` qo'shildi (eski `font-size:0; line-height:0` o'rniga `font-size: 21.6dpx; line-height:1`); JS `renderUiTips` minimal: `el.textContent = el.dataset.text` + `aria-label`. measureTextWidth/renderElementText'ga bog'liqlik yo'q, fonts.ready ham kerakmas (brauzer Onest yuklab Light(300) bilan vizualda ko'rsatadi). Padding/background/position/data-tip-pos qoidalari saqlandi. css v=551→**552**, js v=435→**436**. `node --check` PASS. **TЗ-10-fix** (foydalanuvchi xabari: tooltip chiqmayapti, regress TЗ-10 dan keyin): diagnoz tasdiqlandi — `renderUiTips` faqat `DOMContentLoaded`'da (6059) chaqiriladi, **fonts.ready'da QAYTA chaqirilmaydi**. Onest @font-face yuklangungacha canvas `measureTextWidth` fallback (sans-serif) bilan o'lchaydi → SVG-text `width`/`x=w/2` noaniq → plashka markazlash buzilib tor/siljigan, vizual yo'qolgan ko'rinishida. **FIX**: 6059 dan keyin `document.fonts.ready.then(renderUiTips(document))` qo'shildi (initLineCharts/initGauge naqshi bilan teng). Render-key options.width'ni ham qamrab oladi (js:438) → font yuklanib width o'zgargach avtomatik re-render. CSS `.has-ui-tip:hover > .ui-tip { display: block }` qoidasi joyida (tegmadi). Padding/background tegmadi. js v=434→**435**. `node --check` PASS. **TЗ-12** (mijoz iltimosi): planshet `.dash-page padding 81dpx/171dpx → **64.5dpx/79dpx**` (telefon qiymatlariga teng). Kulrang fon eni mijoz fikriga ko'ra «огромные» edi. Telefon @media qoldi (allaqachon 64.5/79). Karta border-radius va boshqa planshet-spec qiymatlari tegmadi. css v=556→**557**. **TЗ-11-fix-5** (mijoz screenshot: pub planshet .tpub-vac sanaga tushib qoldi): diagnoz — pub planshet'da `.tpub-link/.tpub-vac/.tpub-d1` **absolute pozitsiyada** (link.left=0, vac.left=576.2dpx, d1.left=1158.3dpx — Начало sanasi). TЗ-11-fix da real konteyner eni (`getCellMaxWSpec`) ~1700dpx qaytarardi → vac matn sanasiga (1158.3dpx) ustma-ust tushardi. **FIX**: pub uchun **planshet va telefon yondashuvlari ajratildi** — telefon (block stacked) real konteyner eni; planshet **fix maket-px**: `linkMaxW = 552` (vac.left - link.left - gap = 576.2 - 0 - 24), `vacMaxW = 558` (d1.left - vac.left - gap = 1158.3 - 576.2 - 24). Resume jadval tegmadi. js v=444→**445**. `node --check` PASS. **TЗ-11-fix-4** (mijoz DevTools o'lchov: TЗ-11-fix-3 da line-height Figma spec dan oshirilgan + bottom-tekislik kerak): **Yo'l A — CSS align-self**. (1) JS vac `makLh: 30 → 22` (Figma spec qaytarildi); (2) CSS planshet `.trow-vac { align-self: end }` — vac grid-cell pastiga yopishadi → vac matn pastki chegarasi chap phone pastki chegarasi bilan teng (trow height = max(chap-h, vac-h); chap-h = name + gap + phone ≈ 117dpx ≥ vac-2-qator-h ≈ 62dpx → trow ≈ 117, vac BOTTOM = 117, chap phone BOTTOM = 117). Chap ustun default `stretch` — top'dan name→gap→phone tartibida (tegmadi). Telefon (block) va pub jadval tegmadi. css v=555→**556**, js v=443→**444**. `node --check` PASS. **TЗ-11-fix-3** (mijoz screenshot: planshet 2-qator vac va phone baseline TEKIS EMAS): hisob — chap ustun phone baseline = name-svg-h(~52dpx) + gap(15.8dpx) + phone-y(37dpx) = ~105 real, vac 2-baseline = vac-y(39) + lh(22*TAB_T=49.6) = ~88.4 real → vac 2-qator ~17dpx YUQORIDA. **FIX (Yo'l A — y-koordinasi makLh orqali)**: planshet vac uchun `makLh: 22 → 30` (real: 30 × TAB_T ≈ 67dpx, name-svg-h + gap ≈ 67 ga teng). Telefon (block-layout, vac alohida qator) `22` saqlandi — aralashishlar yo'q. js v=442→**443**. `node --check` PASS. **TЗ-11-fix-2** (mijoz: resume planshet'da .trow-vac matn .trow-name ustuniga TEGIB qoldi): diagnoz — planshet `.trow display:grid (588dpx + minmax(0,1fr))`, **gap yo'q edi**, uzun ФИО («Антонов Дмитрий Иванович») 588dpx 1-ustun chegarasidan oshib vac matniga ustma-ust tushar. Va men TЗ-11-fix da `cellMaxW` ikkala name+vac uchun bitta qiymat ishlatdim (vac eni) → name truncToWidth noto'g'ri ko'lamda hisoblanardi. **FIX**: (1) CSS planshet `.trow` ga `column-gap: calc(24 * --dpx)` qo'shildi (ФИО va Вакансия orasidagi bo'sh joy 24dpx); (2) `.trow-left` va `.trow-vac` ga `overflow: hidden; min-width: 0` qo'shildi (cell-tashqarisi matn-block kesiladi); (3) JS-da `nameMaxW` va `vacMaxW` alohida hisoblanadi: telefon (block) — ikkalasi to'liq qator; planshet — `nameMaxW = 588 - 8` (1-ustun himoya), `vacMaxW = trowSpec - 588 - 24` (2-ustun real eni minus gap). Pub jadval tegmadi. css v=554→**555**, js v=441→**442**. `node --check` PASS. **TЗ-11-fix** (real konteyner eni — bo'sh joy bartaraf): `cellMaxW` STATIK (305/240 × KT) maket-spec dan endi REAL konteyner enidan. Yangi helper `getDpxRealPx()` `--dpx` ni real-px da o'qiydi; `getCellMaxWSpec(cellEl, fallback, phT, isPubRow)` parent `.trow`/`.tpub-row` `getBoundingClientRect().width` → real-px → dpx-spec (real / dpx); planshet resume — 588dpx (1-grid-ustun) ayriladi; telefon yoki pub — to'liq qator eni. Fallback (DOM yangi yoki dpx 0) → eski statik. Ikkala iste'molchi: `applyResumeSearch` `.trow-vac`/`.trow-name` (1 ta umumiy cellMaxW) + `applyPubSearch` `.tpub-vac`/`.tpub-link` (alohida vacMaxW/linkMaxW). Resize'da `renderStaticText` → `initTabView`/`initTabPub` → applyResume/PubSearch zanjir bilan avtomatik qayta-render. js v=440→**441**. `node --check` PASS. **TЗ-11** (mijoz iltimosi 2026-06-24, screenshot): `wrapTwoLines` so'z-bo'yicha → **HARF-harf + DEFIS**. Eski algoritm uzun so'z 1-qatorga sig'masa butunlay 2-qatorga ko'chirardi → 1-qatorda «практически полстроки пустые». Yangi: (1) butun matn sig'sa 1 qator; (2) harf-harf to'ldiramiz `measureTextWidth(slice(0,i+1))`; (3) `str[i]===' '` → tabiiy chegara, defis kerakmas; (4) `str[i-1]===' '` → line1 trim; (5) so'z o'rtasi → `line1 = slice(0,cut) + "-"`, defis ham sig'ishi uchun `cut` 1 harf orqaga (agar kerak); (6) 2-qator `rest` sig'masa `truncToWidth(...)`. `truncToWidth` (so'z bo'yicha 1-qator kesimi) tegmadi — `.trow-name` va boshqa joylar saqlandi. `.trow-vac` va `.tpub-vac` ikkalasi avtomatik yangi mantiq oladi (bitta funksiya). Highlight 8-A/8-X mantiqi: 1-qator → ishlaydi, 2-qator → skip (texnik chegara, defis qo'shilganda ham). Nishonli (mental): «Менеджер по продажам строительных материалов...» → 1-qator harfgacha to'lib «строи-» kabi kesim + 2-qator qoldiq `...` bilan. js v=433→**434**. `node --check` PASS. **TЗ-10** (mijoz e'tirozi: tooltip Light o'rniga 400 chiqadi): diagnoz — `renderUiTips` family `'"Roboto", sans-serif'`; Roboto fayllari TЗ-6 da o'chirilgan → brauzer Arial-fallback → Arial Light yo'q → 400 (Regular) ko'rinar. **FIX**: family `"Roboto"` → **`'Onest', sans-serif'`** (Onest variable 100-900 — Light 300 haqiqiy, kirill bor). JS `renderUiTips` (5763 izoh + 5771 family); CSS izoh (`.ui-tip` 4341) — ikkalasi yangilandi. Padding (8.1/18.9/5.9) tegmadi: cap 0.707 (Onest) bilan cap-top ~15.3, baseline 18, total svg-h 25dpx + 14 padding = ~39dpx — plashka geometriyasi saqlandi. css v=550→**551**, js v=432→**433**. `node --check` PASS. **9-fix-2** (foydalanuvchi screenshot, uzun vakansiya karta chetidan chiqishi): `applyResumeSearch` ichida `.trow-vac` uchun `wrapTwoLines` qo'llandi (tab-pub naqshi) + `.trow-name` uchun `truncToWidth` (`...` bilan kesish). `cellMaxW = (phT ? 305 : 240) * KT` maket-px → dpx. **VAC**: 1 qator → highlight mumkin (renderTabHighlightedText), 2 qator → pipe-split + oddiy renderTabText, **highlight skip** (texnik chegara naqshi, tab-pub 8-X bilan teng). **NAME**: 1 qator + `...` agar sig'masa; highlight saqlangan (truncated matn ichida). Desktop tegmadi (RESUME_COLS.maxW joyida). js v=431→**432**. **9-fix-1** (foydalanuvchi iltimosi): telefon `.tsearch.tsearch--resume` gorizontal padding ×2 (27.92→**55.84dpx**); pub 69.4dpx saqlandi; planshet/desktop tegmadi; h/ico/font/gap tegmagan; R5 fokus mantiqi yangi padding ichida ham to'g'ri (lupa display:none → input chap-padding chetidan boshlanadi). css v=549→**550**. **9** (TЗ 9 — mijoz kuzatuvi 2026-06-17): css v=548→**549**, js v=430→**431**. **9-A** R5 LUPA-FIX tiklandi (`css:4768` blok almashtirildi): yangi qoidalar `.tsearch.is-focused .tsearch-ico, .tsearch.has-value .tsearch-ico { display: none }` + `.tsearch.is-focused .tsearch-input::placeholder { color: transparent }`. Flex-item `display:none` → gap o'z-o'zidan yo'qoladi (sibling yo'q) → input chap chetdan boshlanadi (caret lupa o'rnida). `.is-focused`/`.has-value` klasslari `bindTSearchHandlers` orqali allaqachon o'rnatiladi (JS tegmadi). Eski **6-fix-94** (lupa har doim ko'rinadi) bekor qilindi — mijoz R5 docx talabini tiklamoqchi. **9-B** RESUME_ROWS 6 qatorga to'liq ФИО: Антонов Дмитрий **Иванович**, Борисова Анна **Сергеевна**, Васильев Пётр **Александрович**, Дмитриев Сергей **Викторович**, Иванова Дарья **Михайловна**, Лебедева Наталья **Андреевна**. Boshqa 14 qator 2-so'zli ФИО saqlanadi (realizm). **9-C** uzun vakansiyalar: RESUME_ROWS — 4 qator (Антонов «Менеджер по продажам строительных материалов в магазин Пятёрочка» (mijoz misoli), Дмитриев «Старший инженер-электрик в крупный логистический центр», Ковалёв «Бухгалтер по расчету заработной платы в IT-компанию», Романов «Старший администратор сети розничных магазинов в Нижнем Новгороде»); PUB_ROWS — 3 qator (1: mijoz misoli, 11: Старший администратор..., 14: Бухгалтер по расчету...). ACCOUNTS/account-raqamlari/source tegilmadi (sinxronlik saqlandi). max-width himoyasi (RESUME_COLS.maxW va PUB_COLS.maxW) avtomatik `...` kesilishini ta'minlaydi; clip-path 2-qatorga o'tib ketmaslikni qo'llab-quvvatlaydi. Highlight 8-A/8-X uzun matnlarda `wordPrefixRanges`/`findAllRanges` orqali to'g'ri ishlaydi (substring algoritm matn uzunligiga bog'liq emas). `node --check` PASS. **8-C** (TЗ 8-C — kod-tegilmagan, variant A tanlandi 2026-06-17): PIL skript `tools/measure_tsearch.py` ishga tushirildi va etalon @4x kalibrini hisobladi (mobile-links h≈73.5 maket-px = ~365dpx; tablet h aniq emas — skript top/bot gap detektsiyasi noaniq). JORIY mobile/planshet kapsulalar (6-fix-88/89: telefon ×0.5, planshet ×0.7 mijoz iltimosi) etalon dan ataylam ~50-70% kichik. Mijoz mobile qidiruvni tasdiqladi (kapsula joriy o'lchami unga mos) — etalonga 1:1 qaytarish ONGLI mijoz qarorini buzadi. Joriy CSS saqlandi, versiya bump YO'Q. `tools/measure_tsearch.py` kelajakda foydali (8-D/E yoki yangi etalon-audit uchun). **8-X** (TЗ 8-X — foydalanuvchi qarori 2026-06-17, docx P3/P5 dan ONGLI KENGAYTMA; ОНГЛИ OG'ISHLAR reyestrida qayd etilsin): pub qidiruv MANBA-prefix → **MANBA | VAKANSIYA | URL uchchasida SUBSTRING-moslik** (case-insens.). js v=429→**430**. (1) Yangi `findAllRanges(text, query)` — barcha substring-mosliklarni topadi (URL/vac o'rta-poz. uchun). `wordPrefixRanges` saqlandi (resume uchun ishlaydi). (2) `pubRowMatches` qayta yozildi: src/vac/url uchchasidan biri `indexOf(q) !== -1` bo'lsa `searchOk = true`; `accOk` (popап) AND saqlandi. (3) DESKTOP pub: `applyPubSearch` ichida `.pcell-vac` (size 23) va `.pcell-url` (size 20) ranges'iga ko'ra `renderHighlightedCellText` (oranj #FD6429 fon + oq matn) yoki `renderResumeCellText` (oddiy). (4) MOBILE pub: `.tpub-link` trunc URL'da `renderTabHighlightedText` ranges'i bilan; `.tpub-vac` — `wrapTwoLines` 1 qator hollarda highlight, 2 qator hollarda oddiy (pipe-split highlight keyingi iteratsiyaga qoldi — texnik chegara). (5) RESUME tegilmadi (§4). P2 «Посмотреть» mexanikasi avtomatik ishlaydi (manba so'zi qidiruvga tushib substring orqali manba qatorlarini topadi). `node --check` PASS. Vizual nishonli (kod-only): «м» → 5 Макс+Менеджер(vac)+max.ru(url) birlashtirilgan; «менедж»→Менеджер highlight vac ustunida; «t.me»→5 tg URL highlight; «Сайты»→5 site qatori (highlight yo'q — manba qatorda yo'q); «xyz»→0; ''→27, ✕→27. **8-B** (TЗ 8-B — mijoz mobile pub «не то вышло» fix): js v=428→**429**. Diagnoz: `applyPubSearch` input prioriteti `pcInput.value || mobInput.value` edi, sync `syncToPC` esa bir tomonlama (mobile→PC). Stsenariy: desktop'da `.pub-search` ga qiymat yozilgan → viewport mobile'ga resize qilingan → mobile kapsula bo'sh ko'rinadi, lekin PC qiymati saqlangan → applyPubSearch eski PC qiymati'ga taynuvchan, mobile bo'sh kapsulada "yashirin filtr" → mijoz «не то вышло» (kutmagan natija, mobile'da hech narsa yozmagan bo'lsa ham). FIX: prioritetni almashtirish — **mobile ustun**, PC fallback. Mobile kapsula foydalanuvchi haqiqatan ko'radigan source-of-truth. Resume TEGILMADI (TЗ §4 — applyResumeSearch shu mantiqda qoladi; resume'da hozircha mijoz nuqsoni qayd etilmagan, 8-A highlight ishladi). `pubRowMatches` (manba-prefix), `tabPubRows` (id), `.tpub-row` indeksi va `accountFilterSet` mantiqi to'g'ri — tegilmadi. PUB HIGHLIGHT YO'Q (§2 — kutilgan, manba so'zi qatorlarda yo'q). `node --check` PASS. Vizual nishonli (kod-only tahlil): «м»→5 Макс, «о»→6 Одноклассники, «xyz»→0, ✕→27. **8-A** (TЗ 8-A — mijoz «не подсветились совпадения» fix, mobile resume highlight): js v=426→**428**, css tegmadi. (1) Yangi `renderTabHighlightedText(el, makSize, weight, color, koef, family, ranges)` — desktop `renderHighlightedCellText` ning mobile-spec ekvivalenti (size = makSize × koef, qolgan algoritm aynan: rect+tspan, RESUME_HL_BG=#FD6429 fon + RESUME_HL_FG=#FFFFFF matn, mos bo'lmagan qismlar joriy rangda; render-key + "hl:" — render-key tartibida takror chizmaslik). Pipe-split YO'Q (`.trow-*` 1 qator). (2) `applyResumeSearch` ichida `.trow` qatorlari uchun `.trow-name` va `.trow-vac` ga ranges'ga ko'ra `renderTabHighlightedText` (highlight bor) yoki `renderTabText` (highlight yo'q) chaqiriladi — spec'lar `initTabView` dagi aynan: name → 20/300/#7C7971 (phone) yoki 21/300/#7C7971 (tablet); vac → 20/400/#820407 (phone) yoki 21/300/#7C7971 (tablet); family `'Onest', sans-serif`. (3) `initTabView` oxirida `applyResumeSearch()` chaqirildi — resize/breakpoint-change yo ilk renderда aktiv qidiruv/hisob-filtri mobile .trow larga avto-qo'llaniladi (initTabPub oxiridagi applyPubSearch() naqshi bilan teng). `node --check` PASS. PUB tomonida HIGHLIGHT YO'Q (§2). 6-fix-94 tablet/mobile `.tsearch` lupa-icon typing/fokus paytida YASHIRINMASIN (foydalanuvchi iltimosi): ikkita CSS qoida olib tashlandi — `.tsearch-input:focus + .tsearch-ico { display:none }` (DOM tartibi `icon→input` bo'lganligi sababli adjacent-sibling amaliyotda ishlamasdi) va `.tsearch.is-focused .tsearch-ico { visibility:hidden }` (JS bu klassni focus/blur'da o'rnatadi — endi vizual effekt yo'q, lekin klass saqlanadi — boshqa logika ishlatishi mumkin). Lupa endi har doim ko'rinadi (placeholder yoki kiritilgan matn yonida). JS tegmadi. css v=547→**548**. 6-fix-93/A telefon `.tsearch--pub` padding VIZUAL planshetga tenglashtirildi (variant A): birinchi qiymat 31.5dpx — telefon `--dpx` planshetga nisbatan 2.2× kichik bo'lganligi uchun vizual ~0.7px farq berdi (foydalanuvchi "ta'sir qilmadi"). Yangi qiymat **`padding: 0 calc(69.4 * var(--dpx, 0.0520833vw))`** — proportsiya PHONE_T/TAB_T = 4.962/2.2535 = **2.202**, demak 31.5 × 2.202 = ~69.4dpx → telefonда vizual ~14px chap+o'ng (planshet 31.5dpx × 0.4438 = ~14px bilan teng). Selektor `.tsearch.tsearch--pub` (0020). SABOQ: mobil @media'da planshet-spec qiymatni telefonда vizual takrorlash uchun PHONE_T/TAB_T proporsiyasiga (~2.2×) ko'paytirish kerak — to'g'ridan-to'g'ri ko'chirilsa, telefondaqi vizual ta'sir 2.2× kamayadi. css v=546→**547**. 6-fix-35 snav-logo tooltip dash-main ostida qolib ketishini fix: `.dash-sidebar` z-index `1`→**`2`** (dash-main ham `position:relative z-index:1` — teng bo'lsa DOM tartibida dash-main ustida turardi → sidebar ichidan o'ngga chiqqan tooltip `data-tip-pos="above-center"` plashkasi «Газета «Все Вакансии Нижнего»» dash-main ostida ko'rinmas edi; tasvir-isbot foydalanuvchidan). Sidebar va dash-main yon-yonida (kesilmaydi), z-index 2 vizual layout'ni o'zgartirmaydi — faqat tooltip overflow ustki qatlamga chiqadi. v=544→**545** (js tegmagan). 6-fix-34 BARCHA tooltiplar UI unifikatsiyasi (foydalanuvchi TЗ «bir xil qil»): YAGONA Figma-spek — 21.6/Light(300)/`"Roboto", sans-serif`/#000000, plashka #BDBBB7 (default `.ui-tip`). (1) HTML: `index.html:725` cowboy `ui-tip ui-tip--dark` → `ui-tip`; `index.html:783` exit `ui-tip ui-tip--exit` → `ui-tip`. Boshqa 4 ✕-tooltip (resume-clear/resume-filter-x ikkala panel) allaqachon default — tegmadi. `data-tip-pos="above-center"` cowboy uchun saqlandi (pozitsiya UI emas, joy etalonga bog'liq). (2) JS renderUiTips: cowboy/exit branch'lari OLIB TASHLANDI — yagona spek (21.6/300/Roboto/#000). `anchor: "middle"` + `x: w/2` 6-fix-33 dagi markazlash saqlandi. (3) CSS: `.ui-tip--dark` qoidasi (4341-4345) butunlay olib tashlandi; default `.ui-tip { background: #bdbbb7; padding 8.1/18.9/5.9 }` ham 21.6 uchun mos (cap-top ~17.7, baseline ~26.2). v=543→**544**, js=426→**427** (index.html ikkala ?v= yangilandi). `node --check` PASS. Eski reference: «1.7-fix-1» dark variant tarixiy — agar kelajakda dark qaytarish kerak bo'lsa, git-history dan tiklanadi. 6-fix-33 .snav-exit tooltip yangi Figma-spek + matn markazlash (foydalanuvchi vizual TЗ): (1) HTML class `ui-tip ui-tip--dark` → `ui-tip ui-tip--exit` (dark-cowboy stilidan oddiyga o'tdi, plashka avtomatik default `.ui-tip background:#bdbbb7`). (2) renderUiTips kengaytirildi — `exit` branch qo'shildi: size 14.2→**21.6**, weight 400→**300** (Light), family `DASH_NAV_TEXT_FAMILY`→**`"Roboto", sans-serif`** (Figma-spek), color `#000000`. Cowboy (`--dark`) va default («Очистить поле») branch'lari saqlandi. (3) BARCHA ui-tip lar uchun matn-markazlash: `anchor: "middle"` + `x: w/2` qo'shildi (eski `start`+`x=0` da `measureTextWidth+4` zaxira tufayli matn 4-6dpx chap-yopishib qolardi — vizual muammo). measureTextWidth weight'ni ham tushiradi (`measureTextWidth(t, size, weight, family)`). JS v=425→**426** (index.html ?v= ham yangilandi). DIQQAT: «Roboto» tizimda mavjud bo'lmasa fallback sans-serif — TЗ-5 «Roboto yo'q» qoidasi bu yerda istisno (foydalanuvchi explicit Figma-spek berdi). 6-fix-32 .snav-exit tooltip ko'chirish (foydalanuvchi vizual TЗ, tasvir-namuna: tooltip endi BUTUN tugma ostidan emas, faqat EXIT-ICON ostidan tushadi): `has-ui-tip` klassi `<button class="snav-exit">` dan `<span class="snav-exit-icon">` ga ko'chirildi; `<span class="ui-tip ui-tip--dark">` ham button level'dan icon-span ICHIGA ko'chirildi (svg dan keyin, span yopilishidan oldin). CSS/JS tegmadi — `.has-ui-tip { position:relative }` va `.has-ui-tip:hover > .ui-tip { display:block }` qoidalari avtomatik yangi hostga qo'llaniladi; tooltip `top: calc(100% + 10.5dpx)` icon ostidan 10.5dpx pastda, `left:50%` icon (50×50dpx) markazidan. Versiya bump shart emas (faqat HTML markup, ?v= tegmadi). 6-fix-31 initScrollTop «вверх» SVG markazlash: SVG `<text>` default `text-anchor="start"` (x=0) — text SVG kontener chap chetga yopishar edi (`.scroll-top { align-items: center }` SVG'ni markazlaydi, lekin SVG ICHIDA text chap chetda qolardi). Yangi: `anchor: "middle"` + `x: stWidth/2` → text SVG markazida → tugma ichida 100% gorizontal markazga to'g'irlanadi. 6-fix-30 .acc-sb-btn width 34dpx→**100%** + box-sizing:border-box (SB border-left 1.5dpx qo'shilgach btn parent inner-w (32.5dpx) ga moslashishi kerak — aks holda btn 1.5dpx chap chetga chiqib strelka ↑/↓ gorizontal markazdan siljiydi). 6-fix-29 .acc-sb border-left 3→**1.5dpx** (foydalanuvchi vizual to'g'risi — 50% qisqarish). 6-fix-28 .acc-sb (Group 2103.jpg etalon): SB chap chetida vertikal ko'k separator qaytarildi (`border-left: 3dpx solid #0087fc` + `box-sizing: border-box`). Etalonда SB chap chetida butun balandlik bo'ylab ko'k chiziq bor (btn-up+track+btn-down ni qoplaydi) — header sep bilan teng qalinlik. Track'ning explicit `border:none/outline:none/box-shadow:none` saqlanadi (faqat SB konteyneriga border-left). 6-fix-27 .acc-popup (Group 2103.jpg etalon, S=4): (1) .acc-popup-head border-bottom 1.8→**3dpx** (etalonда y=194..205 sep h=12px / S=4 = 3dpx — header bilan body o'rtasidagi ko'k chiziq qalinroq, 1:1 mos); (2) .acc-sb-track ga aniq `border: none; outline: none; box-shadow: none` qo'shildi — brauzer default/inherited chiziq qoldiqlari bartaraf etildi. 6-fix-26 .acc-popup (Group 2103.jpg etalon, S=4 ichki nisbat): ACC_ROW_H 46.3→**50dpx** (200px/S=4); ACC_VISIBLE 10→**8** qator; .acc-viewport h 463→400dpx (8×50). Header h, popup w, checkbox saqlandi (etalon bilan ~mos). 6-fix-25: .resume-clear-btn left 241.04→**228.04**dpx (foydalanuvchi vizual to'g'rilagan qiymati — ✕ kapsula ichida o'ng tomonda, chetdan ~13dpx ichkari; dizayn bilan 1:1 mos). 6-fix-24: .resume-clear-btn — `right` boshqa qoida bilan override qilingan ekan (foydalanuvchi "left 85% ishladi" deb tasdiqladi); yangi `left: calc(241.04 * var(--dpx))` + `right: auto`. ✕ chap cheti = 271.04 − 30 = 241.04dpx, o'ng cheti aynan kapsula o'ng chetida. 6-fix-23: .resume-clear-btn right -15→**0** (6-fix-22 xato — ✕ tashqariga chiqib ketgan, etalonда esa ✕ kapsula ICHIDA o'ng chetiga ULASHGAN: ✕ o'ng cheti = kapsula o'ng cheti). top=9.9, d=30 saqlanadi. 6-fix-22: .resume-clear-btn (resume+pub search ✕) right 13.5→**-15**dpx — etalonда ✕ markazi kapsulaning o'ng chetida (yarim ichi/yarim tashqari). d=30, top=9.9 saqlanadi. CSS qoidasi ikkala (resume va pub) qidiruvga taalluqli. 6-fix-21 .month-pill aniq Figma spec (w=70.66, h=33.17, Onest 400/18.82/lh100%/ls0%): min-height 33.17dpx (deterministik), padding 0/14.5dpx ((70.66−Май matn-ink)/2), border-radius 9999px (capsule). 6-fix-19 translateY(0.96) G2 saqlanadi. 6-fix-20: .month-pill etalonga moslab (Group 105.jpg, masshtab-mustaqil nisbatlar): padding 2/15→**6/20**dpx (pill h 26→33.5dpx, cap/pill_h 0.52→0.40 etalon); border-radius 18.03→**9999px** (universal capsule, etalon dy=57 da dx=1 → r ≈ pill_h/2). 6-fix-19 G2-tsentr translateY(0.96) saqlanadi. 6-fix-19: vcard-view::after dotted underline matn-ink eni bilan cheklandi. Eski `right:0` svg-eni (measureTextWidth+4 zaxira) tufayli chiziq matn oxiridan oshardi. Yangi: `width: calc(var(--ink-w, 60) * var(--dpx))` + renderMidStaticText har spec uchun `el.style.setProperty("--ink-w", inkW)` o'rnatadi (universal mexanizm — boshqa pseudo elementlar uchun ham foydali). 6-fix-18: reach-badge top −53.5 → **−27.5** qaytarildi (foydalanuvchi vizual tasdiqi — markaz bar tepasida 1:1 mos; etalondan o'lchangan -21dpx ofsetli markaz brauzerda noto'g'ri ko'rinardi). d=65 va bar r=12 saqlangan. 6-fix-17 panel-reach (employment status.jpg etalon, S=3.9975): (1) bar border-radius 20→12 (etalon chap-yuqori burchak parabolasi: y=107.1 dy=3.5 dx=3.5, y=110.6 dy=7 dx=1.3 → r≈12); (2) badge d 55→65 (etalon h=65, badge89 y=49.8..114.8); (3) badge top −27.5→−53.5 (etalondaqi 3 bar markaz_Y - bar_top: −22.3/−21.0/− ya'ni badge markazi bar tepasidan 21dpx YUQORIDA — badge'ning 1/3 ichida, 2/3 panel fonida). 6-fix-16: drop-shadow halo'siz — DUPLICATE-PATH texnikasi (CSS/SVG dropShadow algoritmi round-cap chetida halo qoldirardi). Yangi: defs ga `<filter id="arc_blur">` (faqat feGaussianBlur stdDeviation=3.9), sariq <g> ichida 2 path — pastdan `.gauge-progress-shadow` (stroke=#2D2D2D opacity=0.2 transform=translate(3.9 3.9) filter=url(#arc_blur)) + tepadan asl `.gauge-progress` filter'siz; sariq path pikselma-piksel shadow ustini qoplaydi, halo yo'q. JS ikkala path'ga d ni sinxron yangilaydi. CSS .gauge-progress-g filter olib tashlandi. 6-fix-15: SVG <filter id="filter_arc_drop"> butunlay olib tashlandi (round-cap chetda halo qoldirardi); o'rniga CSS native `.gauge-progress-g { filter: drop-shadow(3.9px 3.9px 3.9px rgba(45,45,45,.2)) }` — brauzer kompozit qatlamida, halo'siz. 38 ID unikal. 6-fix-14: yangi `filter_arc_drop` (faqat drop-shadow qatlami, qora α0.2 dx/dy 3.9, blur 3.9, region -30/-30/376/376) defs ga qo'shildi, sariq <g> ga ulandi. Inner-shadow yo'q — halo qaytmaydi, faqat tashqi soya. 39 ID unikal. 6-fix-13: sariq <g class="gauge-progress-g"> dan filter atribut + defs dan filter1_dii_1_579_arc bloki butunlay o'chirildi (inner-shadow algoritmi chap-chetda halo qoldirardi). Sariq yoy flat #FFCD2A stroke 15.6225 round-cap. Knob shisha effekti tegilmagan. 38 ID unikal. 6-fix-12: filter1_dii_1_579_arc region x/y/width/height → -30/-30/376/376 (viewBox 316'dan 30px tashqariga) — chetdagi oq halo bartaraf, drop+inner shadows saqlangan. 6-fix-11: sariq filter1_dii_1_579_arc dan 3-qatlam (krem-oq #FFFEDD α0.4 inner-shadow «effect3») 6 ta tag bilan o'chirildi (feColorMatrix SourceAlpha + feOffset 2.6/2.6 + feGaussianBlur + feComposite + feColorMatrix matrix oq + feBlend). Drop (effect1) va sariq-qizg'ish inner (effect2) saqlandi — 3D shisha effekt qoladi, brauzer yor'titgan oq chiziq ketdi. 6-fix-10: gauge matn yangi Figma-spec — prefix 44/100/ls-4%/#7C7971, value 44.67/400/ls-4%/#2F3027, sub 19.24/300/ls-4%/#7C7971. Yig'indi-markaz mantiq (prefixW+gap+valueW)/2 textCx=230 atrofida. Sub-matn class="gauge-sub" + anchor middle x=230. Ko'k track <g> dan filter atribut + defs dan filter0_ii_1_579_arc bloki o'chirildi (oq inner-shadow chizig'i edi); sariq filter1_dii_1_579_arc saqlangan. 6-fix-9 gauge: assets/figma/gauge-arc.svg (316×316, ko'k+sariq yoy, inner+drop shadows) + gauge-knob.svg (55×55, sariq halqa+oq markaz, chiroqcha) Figma SVG'lari inline-ko'chirildi. Filter ID lariga _arc/_knob suffiks (grep nizolar yo'q, 40 ID unikal). Outer transform translate(75.08 50.08) scale(0.96828) — Figma viewBox 316 → bizning eski bbox (cx=230, cy=205, r=143.5 dpx ekvivalent). GAUGE_GEOMETRY Figma viewBox-da: cx=160, cy=160, r=148.2, 135.1°→405.1° (270°). Knob G transform (circle cx/cy emas): knob @380/500 = (299.5, 110.0) Figma'da. Sariq yoy dinamik (JS qayta yozadi), ko'k yoy statik (JS tegmaydi). Matn «На»→«на» (data-label-prefix, aria, prefix textContent). 6-fix-8-REVERT-2: gauge yoy qamrovi ham eski 270° ga qaytarildi (startDeg 132.4→134.8, endDeg 344.5→405.1; HTML static path/knob endpoint'lar (365.0, 156.5) va (128.9, 306.8) ga qaytarildi). Gauge butunlay 6-fix-8 dan oldingi holatda. 6-fix-8-REVERT: gauge eski qiymatlarga qaytarildi (cx=230, cy=205, r=143.5, valueSize=44, sub y 246/270, 00/500 size 16, rotated 17, knob r=12.5/8) — kalibr xato (yakka @hq eksport masshtabi). YAGONA o'zgarish: yoy qamrov 270.3°→212.1° (startDeg 134.8→132.4, endDeg 405.1→344.5; hero_block@hq.png 1600×423, S=1.016 etalonidan). 6-fix-8 gauge (gauge-new@hq, S=2.2647): cx 230→240.5, cy 205→209.3, r 143.5→142.6, qamrov 134.8°→405.1° (270°) → 132.4°→344.7° (212.3°), thumb r outer 12.5→21 / inner 8→13.4 (dug'ani uzib turadi), markaziy matn 33+44 → 80+80 ikkalasi #2F3027 bir baseline (232), sub y 246/270→259/283, 00/500 16→18 (y 329→336), aylangan yorliqlar 17→14, track #EEF8FA→#EDF6F8. 6-fix-5/6/7: pill svg translateY(0.96) G2-markaz; .month-label spec → :not(.promo-month) (panel C oylari ikkala klassda — kulrang qayta-bo'yash to'xtatildi, #2F3027 amalda); .resume-cols = linear-gradient(90deg,#E7E5E3,#EBE2C8) r14.02 (resume+pub umumiy). 6-fix-4 panel B (panel-reach-new@hq, S=3.9975): badge d66→55 markaz=bar tepasi (top −27.5), shisha-gradient 2-baza usulida (222,218,212/.87 → 158,152,137/.75 → 98,95,91/.74), badge-matn 21→16.3; bar balandliklari KVADRATIK reachBarHeight(p)=88.93+4.915p−0.0283p² (302.2/214.1/172.1); «450» 32.61→28.7; underline ru2'dan: w 257.8, nuqta 5.1/qadam 10.2, #D2D1CF, mt 21, ml −19 (chetdan x=4). Barlar/yorliqlar/Более/title — o'zgarmagan (etalon tasdiqladi). 6-fix-2: kontent-bloklar radius 15.59 (.hero-panel/.ws-panel/.vcard 11.19→, .resume-panel 18.8→; .dash-card 18.8 qoldi). DIQQAT: CSS/HTML prettier bilan qayta formatlangan (2-space, double-quote) — eski anchor'lar mos kelmasligi mumkin. 6-fix (vcard-single@hq/vcard-view-hover@hq): vcard sarlavhalari 2/3/4-kartalarda 1 QATOR (faqat «Количество ссылающихся|сайтов» 2 qator — image8 ✓); foot-bottom 9.1→8.81; «Посмотреть» baseline = doira-top+21.5 (margin-top 9.2); punktir 1.48/3.0 top 16.2 (baseline+4.15). Dot 13.5/d17.6/x8, title 46.4/12.09, num 85.5/26.81, doira d33.8 — etalon tasdiqladi, o'zgarmadi. 5-fix-3: .vcard-view 12.09→14.09 (svg-baseline 10→12: margin-top 10.6→8.6, punktir top 15.5). TЗ-6: stacklar 'Sora'/'Figtree', 'Onest', sans-serif; highlight-cap 0.707; sig'ish Onest-kirill bilan isbotlangan (yagona istisno ttable-title L1 +1.3% — 3-FIX shrink ushlaydi). 5-fix-2: desktop .snav-btn:not(.active):hover = #EEE5CE fon + inset-kontur singiydi, transition 0.18s. 5-fix: panel A oy-yorliqlari .month-label/.month-pill 25→18.82/400/lh100% (Figma-spec, foydalanuvchi qarori; .promo-month 22 tegilmagan). TЗ-5: barcha stacklar 'Sora'/'Figtree', Arial, sans-serif (Roboto yo'q); JS: DASH_NAV/REPORT/STATIC_TEXT_FAMILY Arial bilan, TAB_DIGIT_FAMILY="Arial" (eski renderTabRoboto→renderTabDigits), highlight cap 0.711→0.716. Sig'ish Arial bilan qayta isbotlangan (resume/pub ustunlar, sanalar, stat-raqamlar 254.8/256 — 2.4-FIX qaytmadi); faqat 2 planshet-sarlavha (600→Arial Bold) +2-3% — 3-FIX max-width himoyasi proporsional siqadi. GLOW'lar (4-fix-3): filter:blur YO'Q (Firefox ~100px cheklovi) — 3 glow radial-gradient, Gauss-stoplar (I(t·R)/I(0), R=yarim-o'q+2.2σ): GLOW-1/karta-zaif σ=587.5 peak .259 (markazlar (1231,518)/(1768,1121.5), box 3133×3860); GLOW-2 σ=400 peak .399, markaz (101,1072), vertikal radius 672 ga SIQILGAN (ko'rinadigan tepa y=400 — snav ~350 dan pastda; 4-fix-2 shu yerda).
js/script.js          — v=353 (keyingi bump: 354)
assets/figma/*.svg    — rasmiy ikonkalar (cowboy, nav-*, stat-*, search-lupa, dots-menu, terminal-logo...)
fonts/                — sora-variable-latin (100-800), figtree-variable (300-900), onest-variable-cyrillic/latin (100-900, TЗ-6). KIRILL = ONEST: cap 0.707, x-h 0.527, og'irliklar haqiqiy (variable). RAQAMLI kalibr-renderlar (TAB_DIGIT_FAMILY: tab-pub sanalari, stat-raqamlar) = ARIAL (Onest raqamlari non-tabular va keng: '246' 1.812em vs 1.669 — col4 268.8>256, sana 108.7>107.2 sig'maydi)
docs/design/          — etalonlar: Homies Lab_ru2.jpg (4×1920 to'liq maket), panel-*@4x, vcard@4x,
                        search-states@1x, states-sheet@1x (1072×641), cowboy-tooltip@1x,
                        tablet-resume@4x (4×852), tablet-links@4x (2-bosqich), mobile-resume@4x (4×387),
                        mobile-links@4x (2-bosqich)
docs/ТЗ_реестр_требований.md — qabul cheklisti (R/G/POP/SEL kodlari)
```
- **dpx tizimi**: 1 dpx = dizayn-px @1920 = `0.0520833vw` (CSS fallback); JS runtime'da `--dpx`ni px qiymat sifatida har resize'da yangilab turadi (`updateZoomAwareLines`). Hech qachon --dpx'ni qo'lda o'rnatmaslik.
- **Mobil kalibrlar**: planshet maketi 852px → `TAB_T = 1920/852 = 2.2535`; telefon maketi 387px → `PHONE_T = 1920/387 = 4.962`. Mobil o'lchamlar CSS'da tayyor ko'paytirilgan dpx, JS'da maket-px × koef.
- **Breakpointlar**: `@media (max-width: 799.98px)` planshet (desktop bloklar yashirinadi, `.tab-view` ochiladi), `@media (max-width: 489.98px)` telefon (planshet ustiga override). Ikkala chegara kesilishida `matchMedia.change → renderStaticText()` (to'liq qayta-render).
- Layout: `.dash-page > .dash-card(overflow:hidden, glow ::before/::after) > .dash-top(flex: sidebar+main) + .resume-panel + .tab-view + #pageSpacer(dinamik)`.

## 2. Dvigatel va kengaytmalar (js/script.js)
Yadro: `renderElementText(el, {text, lines, size, width, height, x, y, anchor, weight, color, family, lineHeight, letterSpacing, useBitmapText:false})` → SVG `<text>`; `measureTextWidth(text,size,weight,family)` (canvas, natija = ceil+2); render-key (`getSvgTextRenderKey`) takror chizishni kesadi. Birinchi baseline = `round(0.82·size)`; cap: kirill→Roboto 0.711·size, lotin/raqam→Sora 0.753·size. Fontlar: `DASH_NAV_TEXT_FAMILY` = Sora-stack (kirill Roboto'ga tushadi — ataylab), `REPORT_TEXT_FAMILY` = Figtree-stack.

`renderStaticText()` (DOMContentLoaded + fonts.ready + breakpoint-change'larda) oqimiga ulanganlar:
- `renderDashboardStaticText` — sidebar nav/bell/exit matnlari.
- `renderHeroStaticText` + `initGauge` — hero matnlari va parametrik gauge (`.hero-gauge[data-value/max]`).
- `renderMidStaticText` (midTextSpecs) — o'rta qator panellari A/B/C + vcard matnlari.
- `initLineCharts` — `.line-chart[data-values...]` chiziq-chartlar; `initReachBars` — `.reach-bar[data-pct]` ustunlar (h=143.1+1.447·pct).
- `initResumeTable` — desktop jadval DOM (RESUME_ROWS→qatorlar) + matnlar; `renderResumeCellText`/`renderHighlightedCellText` (oranj #FD6429 highlight rect+tspan).
- `initResumeSearch` — prefix-qidiruv (`wordPrefixRanges` — faqat so'z boshi, case-insensitive), filtr, fokus holatlari (.is-focused/.has-value), ✕ tozalash; `applyResumeSearch` = qidiruv ∧ hisob-filtri + popап ochiq bo'lsa SEL1 qayta hisob.
- `initAccountsPopup` (2.3 dan: FABRIKA) — `createAccPopupController(cfg)` → 2 instansiya: `resumeAccPopup` (head '.resume-panel:not(.pub-panel) .resume-head', ID'lar accPopup/accList/accThumb, apply=applyResumeSearch) va `pubAccPopup` (head '.pub-head', pubAccPopup/pubAccList/pubAccThumb, apply=applyPubSearch). ACCOUNTS YAGONA manba, har kontroller O'Z filterSet/scrollIndex/bound/listBuilt; `accountFilterSet` = resumeAccPopup.filterSet (shim — applyResumeSearch tegilmagan). open() boshqalarini yopadi (bir vaqtda 1 popап); POP7 o'z popап/dots'idan tashqari click → yopish; filtr-✕ faqat O'Z ro'yxatini tozalaydi; mexanika 1.6 aynan (layoutAccPopup --acc-w/--acc-date-x, scrollbar, POP8 filter-active CSS pub-head'ga avtomatik, SEL1 ensureAccPopupVisible ikkala apply'da). addAccountEntry ikkala ro'yxatni markListStale qiladi.
- `initTabView` + `renderTabText(el, makSize, weight, color, makLh, koef)` — mobil ko'rinish: nav/statlar/kompaniya/jadval matnlari (koef=TAB_T yoki PHONE_T; PHONE_T'da `data-text-phone` varianti tanlanadi), `formatPhoneIntl` (+7 XXX XXX-XX-XX).
- `initTabPub` (2.4) — adaptiv pub-jadval `.tab-pub` (PUB_ROWS yagona manba): sarlavha 25/600 lh30.3 (tel 20/600 lh24.1), sub 20/300 (tel 18, 2 qator statik), shapka 14/400 (tel «Вакансия» yashirin, «Начало/End»), qator h65m (tel stacked h138m), ajratkich OQISH rgba(255,255,255,.66) 2m; havola PROTOKOLSIZ `truncToWidth` bilan «...» (planshet 238m, tel 305m), vakansiya `wrapTwoLines` 2 qatorgacha; **sanalar Roboto** (`renderTabDate`, 20: etalon cap 14.4/w 99.5 = Roboto tabular, Sora sig'masdi), `pubDateDots` («25 Янв 2025»/«25 Апреля 2025»→«25.01.2025», 3-harf prefiks lug'ati); `copyToClipboard` (clipboard API→execCommand fallback, feedback YO'Q — TODO-CLIENT); `applyPubSearch` adaptiv qatorlarni ham filtrlaydi (`pubRowMatches` umumiy predikat — desktop bilan bitta holat). Ko'rinish: media ichida body.pub-mode → .ttable yashirin, .tab-pub block; adaptivda qidiruv kapsulasi YO'Q (1.8 resume'da ham yo'q — naqsh saqlangan).
- `initPubTable` (2.1) — «Дополнительные публикации» jadvali (PUB_ROWS→#pubRows, resume CSS-klasslari umumiy; per-ustun baseline: vac 50.5 / raqamli 49.6); `initPubSearch` — pub-kapsula fokus/✕ + real-time filtr; `initPubToggle` — DEV: hash `#pub` → body.pub-mode + snav aktiv almashinuvi (3-bosqichgacha vaqtinchalik).
- `applyPubSearch` (2.2) — PUB_SOURCE_NAMES {ok:Одноклассники, vk:ВКонтакте, site:Сайты, tg:Телеграм, max:Макс} display-nomi BOSHIDAN prefix (case-insens.), highlight YO'Q; bo'sh→27, mos yo'q→0. `initViewCards` (P1/P2) — vcard «Посмотреть»+strelka juftlik-hover (CSS: .vcard-foot pointer-events:none, bolalari auto → bo'sh oraliq hover bermaydi; aktiv: punktir 1.26/2.53 top13.5 #161715, doira #2F3027, strelka oq — pressed ham shu, states-sheet'da alohida rang yo'q); klik → data-pub-word so'zi pub-qidiruvga (to'liq almashtirish) + hash #pub fallback (// TODO-STAGE3).
- `renderUiTips` — generik tooltip matnlari (`.ui-tip`; `.ui-tip--dark` = ixcham to'q #64635C/oq 14.2 — kovboy+exit; oddiy = #BDBBB7/qora 22); pozitsiyalar: default below-center, `data-tip-pos="below-right"` (+15.7, gap 21.8), `"above-center"` (gap 7); ko'rsatish sof CSS `.has-ui-tip:hover`.
- `initUiTipClamp` — mouseover'da tooltip gorizontal clamp (karta/viewport chetidan chiqsa marginLeft bilan suriladi).
- `initScrollTop` — «вверх»: chegara desktop 400 / mobil(<800) 100; visualViewport.resize'da `innerHeight − vv.height > 150` → klaviatura ochiq deb tugma yashiriladi (faqat mobilda; vv yo'q brauzerda fallback — oddiy xatti-harakat); breakpoint change → sync.

## 3. Data massivlari (hammasi js/script.js ichida)
- `RESUME_ROWS` — 20 yozuv {name, phone(89XXXXXXXXX), vacancy, email, replyDate, account, accountDate}. Familiyalar А..Х 20 turli harf, vakansiyalar 20 turli harf; R4: №3 Васильев va №12 Морозов umumiy hisob `Св000000001829`.
- `RESUME_COLS` — 7 ustun {key, x(panel-rel), size, maxW(himoya)}: x=[49,322.8,540.2,896.2,1181,1373.5,1621], size=[23,23,20,20,20,18.82,20], weight 300, rang #2E2F27; baseline qator ichida 50.5, qator h 89.
- `ACCOUNTS` — 20 yozuv {num, date DD.MM.YYYY} SANA BO'YICHA KAMAYISH; 19 tasi jadval hisoblari + `Св000000001944` (resume-jadvalda yo'q); `accountInsertIndex/addAccountEntry` tartibni saqlab qo'shadi.
- `PUB_COLS`/`PUB_ROWS` (2.1) — pub-jadval: 6 ustun {key,x,size,base,maxW}: x=[46.8,589.4,789.1,1034.3,1234.4,1581.1], size=[23,20,18.82,20,20,20], base=[50.5,49.6…]; header `.pcol-1..6` band-rel x=[18.8,559.2,759.6,1005.8,1203.4,1543.5]. 27 yozuv {vac,placed,account,accDate,url,removed,source}; source ok6/vk6/site5/tg5/max5; 7 hisob 2 tadan (1829/1846/1874/1902/1917/1930/1944), barcha 20 ACCOUNT ishlatilgan, accDate ↔ ACCOUNTS sanasi mos; placed «11 Нояб 2024» (qisqa oy) KAMAYISH tartibida, accDate/removed to'liq oy; etalon qatori t.me/Rabota_vod/273 + Св…1829 saqlangan.
- `heroTextSpecs`/`midTextSpecs` — selektor-spec jadvallar (size/weight/color/lineHeight/family:'figtree') — renderHero/MidStaticText ularni renderElementText'ga uzatadi.
- Mobil kegllar initTabView ichida shartli: tablet/phone juftliklar (nav 19/16, stat-num 47/45, label 16.5/16, kompaniya 34/21+25/19, title 25/20.5 (600), sub 22/19, headerlar 18.5/14, yozuvlar 21/18; telefon vakansiya QIZIL #820407, tel #1F76A3).

## 4. Asosiy ranglar/tokenlar
#2E2F27 (asosiy to'q matn), #7C7971/#7B786F (kulrang), #FD6429 (highlight), #1F76A3 (tel ko'k), #820407 (mobil vakansiya qizili), #FE1D19 (filtr qizili), #0087FC oilasi (popап ko'klari: kontur/checked; #7BBEF9 noaktiv, #026FCE hover, #86C7FF thumb), #B1AFAA/#7C7971 (✕ normal/hover), #BDBBB7 (oddiy tooltip), #64635C (dark tooltip), #2F3027/#40422E (olive karta normal/hover), #EEE5CE (mobil tugma hover), #DD5326 (exit).

## 5. Desktop regress-suite (har adaptiv-dokладда majburiy)
Python bilan tekshirish — `css.split('@media (max-width: 799.98px)')[0]` (desktop qismi) ichida:
1–7) `.rcol-1..7 { left: calc(X` X=[32.3,290.7,511,874,1150.7,1344,1601.5];
8) JS `RESUME_COLS` x = [49,322.8,540.2,896.2,1181,1373.5,1621];
9) `.resume-col` absolute top 13.8; 10) `.resume-cell` absolute + `clip-path: inset(calc(-8`;
11) `.resume-row` h calc(89; 12) `.resume-cols` band w 1743.5 h 48.8;
13) `.resume-filter-btn` absolute left 1724.1 top 32.3; 14) `.resume-search` left 1442 top 31.15, 271.04×49.85;
15) `.tab-view { display: none; }` media TAShQARISIDA; 16) tnav/tstats/trow qoidalari FAQAT media ichida;
17) planshet blokida `grid-template-columns: calc(418` saqlangan.
Qo'shimcha: telefon qoidalari faqat 489.98 ichida; `node --check`.

## 6. Saboqlar (takrorlamaslik!)
- **slice-to-EOF TAQIQ**: `s[s.index('@media...'):]` bilan almashtirish media'dan keyingi desktop qoidalarni yutgan (1.8-REGRESS). Har doim aniq anchor-to-anchor.
- CSS tartibi tarixiy aralash: 1.4 jadval qoidalari 1.5–1.8 bloklaridan KEYIN ham bor — qidirishda butun faylga grep.
- Roboto VARIABLE (100-900) — 600 haqiqiy bold, advance kengayadi; sig'ish hisoblarini fontTools instancer bilan kerakli weight'da qilish.
- Etalon JPEG ranglari aldashi mumkin (eski image5 sariq karta — yolg'on edi); yuqori sifatli @4x etalon ustun.
- measureTextWidth natijasi = ceil(w)+2 (zaxira ichida).
- fill SVG-atributi CSS qoidasidan yutqazadi — hover/checked rang almashinuvi qayta-rendersiz (`.acc-row:hover .svg-label text {fill}`).
- `.dash-card overflow:hidden` — popап/elementlar kartadan chiqsa kesiladi; sahifani uzaytirish kerak bo'lsa spacer KARTA ICHIGA.
- Sheet-kolajlar (states-sheet) masshtabi nomuvofiq (1.6–2.0) — ichki kalibr (matn cap ↔ ma'lum kegl) yoki ma'lum element orqali.
- Bash heredoc uzun bo'lsa "unexpected EOF" — python skriptни faylga yozib ishga tushirish; print'da cp1252 → `sys.stdout.reconfigure(encoding='utf-8')`.

## 7. TODO-CLIENT markerlari (mijoz qiymatlari kelganda almashtirish)
- index.html: kovboy tooltip matni («Газета «Все Вакансии Нижнего»» vaqtincha) — 2 komment.
- css/style.css: `.snav-logo:hover svg { filter: brightness(0.93) saturate(1.15) }` vaqtincha hover — 2 komment.
- Ochiq savollar: panel B punktir (Figma kutilmoqda), GLOW-2/karta-glow aniq spec, panel A/C/vcards/jadval fill'lari (hozir rgba(255,253,244,.62)), jadval band qoplamasi rgba(150,140,110,.12), mobil 432 yorlig'i «Всего дополнительных размещений» (semantika savoli), «Терминал» faqat planshetda ko'rinadi (telefon bandda logosiz — etalon).

## 8. JORIY HOLAT (keyingi qadam)
- 1.8/2.1/2.2/2.3 QABUL QILINGAN. **2.4 BAJARILDI** (css v=436, js v=349): adaptiv pub-jadval .tab-pub (planshet+telefon, etalonlar tablet-links@4x/mobile-links@4x); suite'lar `tools/regress_24.py` (17+30 PASS) va `tools/tabpub_fit_check.py` (ALL FIT). DOKLAD berilgan, qabul kutilmoqda.
- 2.4 saboq: tab-pub SANALARI etalon bo'yicha Roboto-raqam (tabular) — Sora-raqamlar nopproporsional/keng (03.09.2025: Sora 110.8 vs Roboto 101.7 vs etalon 99.5); mobil «Начало End» etalon'da deyarli tegib turadi (End o'ng cheti kontentdan ~2m chiqadi — dizayn shunday).
- **6-FIX-8 SABOQ (HECH QACHON ESDAN CHIQARMA):** YAKKA @hq EKSPORT (faqat element kesilgan, gauge-new@hq.png, vcard-single@hq, panel-reach-new@hq) bbox'idan TO'G'RIDAN-TO'G'RI MASSHTABLAMA — masshtab koeffisienti har eksportда BOSHQACHA va element-ichi-piksel != bizning panel-ichi-dizayn-px. **Har doim KONTEKSTLI etalon** (hero_block@hq, panel'ning to'liq blok eksporti, ru1/ru2 Homies Lab to'liq maket) orqali kalibrla: shu yerda elementlar bizning hero/panel ichida turibdi, va S = piksel-eni / panel-dizayn-eni aniqlanadi. 6-FIX-8'da gauge-new@hq (1155×836, S=2.2647) bo'yicha skall qildim → gauge bizning panel'dan chiqib ketdi. **6-FIX-8-REVERT:** hero_block@hq.png (1600×423, S=1.016) bo'yicha qaytadan o'lchadim → eski cx=230/cy=205/r=143.5 to'g'ri ekan, faqat YOY QAMROVI 270.3°→212.1° (132.4°→344.5°) o'zgartirildi.
- **2.4-FIX BAJARILDI** (css v=437, js v=350): tstat raqamlari ham ROBOTO (45 planshet / 46 telefon — etalon cap 31.8/32.8 va ink-enlar isbotladi; Sora 47/45 ~14% keng bo'lib planshet col4 da DOIMIY (290>256), telefonda scrollbar-sharoitda «246» ni ag'darardi — vw scrollbar'ni o'z ichiga oladi, konteyner real eni esa yo'q, −84..105dpx); `renderTabDate`→`renderTabRoboto` (sanalar+raqamlar); chip margin etalon: 19.8dpx planshet / 74dpx telefon (ink-gap − raqam lsb); `.tstat{white-space:nowrap; font-size:0}` (struktura himoyasi + probel-tugunlarning real-px hissasi o'chirildi); telefon `.tth-vac{vertical-align:top}` (baseline-siljish: 856 vs 852 → 854=854=854). Suite `tools/regress_24fix.py` 17+17 PASS; headless DOM sonli-o'lchov bilan tasdiq (700/390/320 + 17px scrollbar-sim + #pub).
- MUHIM o'lchov-saboq: Homies Lab_ru.jpg (7752×6000, ×4.0375) O'NG chetida ~21.6px **scrollbar запечён** (karta 41.6..1856.8, 1898.4 ichida markazlangan) — o'ng-anker elementlar (kapsula/«...») chapga surilgan ko'rinadi; ular uchun resume Figma-spec qayta ishlatildi, chap-anker (title/ustunlar) etalondan.
- **3-BOSQICH BAJARILDI** (css v=438, js v=351): S1 — `setPubMode(on)` (klasslar+hash replaceState+initTabView tnav-rang uchun; filtr/qidiruv holatlariga TEGMAYDI), sidebar `.snav-btn` va mobil `.tnav-btn` ads/resume kliklari bog'langan (journal/email harakatsiz); B1 — `.promo-arrow` klik → pub + `scrollPubTitleTop()` (desktop .pub-title / adaptiv .tpub-title ekran tepasiga, smooth; hover TODO-CLIENT); «Посмотреть» endi setPubMode(true)+so'z; TODO-STAGE3 yopildi, hash dev-yo'l sifatida ishlaydi (hashchange listener). YANGI QOIDALAR: brauzer-MCP TAQIQ, tekshiruv minimal (node --check + nishonli), doklад 6-8 qator.
- **3-FIX** (css v=439): (1) planshet ttable/tpub title-sub svg'lariga `max-width:100%` himoya (etalon'da title ink 1386/1410 — chetga 5.6m, render-zaxira/kasr-piksel chiqaradi; viewBox meet → yomon holatda proporsional siqiladi); (2) promo-arrow hover = «Посмотреть» uslubi (doira #2F3027/oq strelka) + transition .18s — TODO-CLIENT yopildi; (3) telefon `.tpubh-end` 1406.8→1418 (etalon ink-oraliq «Начало|End» = 5m).
- **4-BOSQICH BAJARILDI** (v=440/352): TD1 — kovboy SVG dedupe (defs + 7×`<path id="cb0..6">` + `<g id="cbAll">`, masklar/filter/stroke'lar `<use>` orqali; 179.5KB→24.2KB, index 261.9→106.5KB, path-data baytma-bayt saqlangan); G2-audit toza (y=round(0.82s) yagona formula; resume raqam-ustunlari ru2-etalon bo'yicha umumiy baseline 50.5 — hujjatlangan istisno; pub/tab-pub raqam-qoida qo'llangan); PIXEL-PASS 12 nuqta ✓; favicon data:, (konsol-404 o'chdi). Deploy-to'plam: index.html + css/ + js/ + fonts/ (20 fayl) + icons/clear/ (2 svg); tools/, docs/, assets/figma (faqat kommentlarda), image*.png — deploy'ga KIRMAYDI.
- **4-FIX** (v=441/353): tnav hover etalon (fon #EEE5CE, kontur fonga singiydi — border-color ham #EEE5CE, transition .18s, `:not(.is-active)`); krossbrauzer: `<use>` xlink:href dublikatlar (28) + xmlns:xlink, `bindMqChange` (addListener fallback), `smoothScrollTo` (scrollBehavior detect), `inset:0` → longhand (9 joy), `-webkit-clip-path` dublikat; flex-gap Safari ≥14.1 talab (zamonaviy maqsadga mos, o'zgarmadi); iOS fokus-zoom viewport maximum-scale=1 bilan allaqachon o'chiq.
- TODO-CLIENT: «Скопировать» feedback; tab-pub sub semantikasi.
- Xotira fayli: `C:/Users/admin/.claude/projects/C--Users-admin-Desktop-header-only/memory/vsevn-dashboard-arch.md` — to'liq arxitektura tarixi (o'qish tavsiya etiladi).
